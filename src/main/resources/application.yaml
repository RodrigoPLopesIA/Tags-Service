
server:
  port: 8000
spring:
  application:
    name: tags-service
  data:
    redis:
      host: redis
      port: 6379
    mongodb:
      uri: mongodb://root:root@localhost:27017/notesdb?authSource=admin
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/notes-realm
management:
    endpoints:
        web:
          exposure:
            include: health,info,metrics,env

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8671/eureka