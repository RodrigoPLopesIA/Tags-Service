version: "3.9"

services:
  tags-service:
      container_name: tags-service
      image: tags-service
      ports:
        - 8082:8082
      networks:
      - microservices-net

networks:
  microservices-net:
    external: true